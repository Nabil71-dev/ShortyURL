// import { NextRequest, NextResponse } from "next/server";

// const adminRoutes = ["/admin/dashboard", "/admin/client-list"];

// const AdminRouteVerify = (req) => {
//     if ("user doesnot exist" && adminRoutes.includes(req.nextUrl.pathname)) {
//         const absUrl = new URL("/", req.nextUrl.origin);
//         return NextResponse.redirect(absUrl.toString());
//     }
// }

// export default AdminRouteVerify;